apiVersion: v1
kind: Service
metadata:
  name: eventalpha-intraday-service

spec:
  selector:
    app: eventalpha-intraday-api

  ports:
    - protocol: TCP
      port: 4000
      targetPort: 4000

  type: LoadBalancer